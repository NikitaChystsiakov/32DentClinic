<?php
/**
 * Настройки отправки заявок в Telegram. Скопируйте в config.php рядом и
 * заполните — config.php не хранится в репозитории (.gitignore), он лежит
 * только на сервере. Как получить токен и chat_id — docs/ФОРМА-ЗАПИСИ.md.
 */
return [
    // Токен бота от @BotFather, вида 123456789:AAH...
    'bot_token' => '',
    // ID чата или пользователя, куда слать заявки (узнать у @userinfobot).
    // Чтобы переключить заявки на клиента — заменить только это значение.
    'chat_id' => '',
    // Часовой пояс для строки «Время» в сообщении.
    'timezone' => 'Europe/Minsk',
];
